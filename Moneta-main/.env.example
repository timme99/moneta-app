# ============================================================
# Moneta-App | Umgebungsvariablen
# Kopiere diese Datei zu .env und trage deine Werte ein.
# WICHTIG: .env niemals committen (ist in .gitignore)!
# ============================================================

# ── Supabase (Moneta-App Projekt) ────────────────────────────
# ACHTUNG: Strikt die Keys des Moneta-App Projekts verwenden –
#          nicht die Keys des zweiten Supabase-Projekts!
#
# Zu finden in: Supabase Dashboard → Moneta-App Projekt → Settings → API

MONETA_SUPABASE_URL=https://<deine-project-ref>.supabase.co
MONETA_SUPABASE_ANON_KEY=eyJ...              # "anon public" Key
MONETA_SUPABASE_SERVICE_ROLE_KEY=eyJ...      # "service_role" Key – NUR serverseitig!

# ── Browser-seitig (Vite – muss VITE_ Prefix haben) ──────
# Gleiche Werte wie MONETA_SUPABASE_URL / MONETA_SUPABASE_ANON_KEY –
# aber Vite exponiert nur Variablen mit "VITE_" Prefix in den Browser-Bundle.
VITE_SUPABASE_URL=https://<deine-project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...               # "anon public" Key (identisch mit MONETA_SUPABASE_ANON_KEY)

# ── Google Gemini API ────────────────────────────────────────
# https://aistudio.google.com/app/apikey
GEMINI_API_KEY=AIza...

# Optionale Ratenlimits pro Funktion pro Tag (0 = deaktiviert)
GEMINI_LIMIT_ANALYSIS=10
GEMINI_LIMIT_CHAT=30
GEMINI_LIMIT_NEWS=15
GEMINI_LIMIT_RESOLVE_TICKER=30
GEMINI_DAILY_CAP=0

# ── Alpha Vantage via RapidAPI ───────────────────────────────
# https://rapidapi.com/alphavantage/api/alpha-vantage
RAPIDAPI_KEY=...

# ── E-Mail Service (Resend) ──────────────────────────────────
# https://resend.com/api-keys
RESEND_API_KEY=re_...
FROM_EMAIL=Moneta <onboarding@resend.dev>

# ── App-URLs & Secrets ───────────────────────────────────────
APP_URL=https://deine-app.vercel.app
CRON_SECRET=min16zeichenlangesSecret
